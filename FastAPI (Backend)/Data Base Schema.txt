CREATE TABLE users (
    ID INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    password TEXT,
    type INTEGER,
    email TEXT,
    user_id INTEGER,
    phone TEXT UNIQUE
);

CREATE TABLE food (
    ID INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    quantity INTEGER,
    location TEXT,
    status BOOLEAN
);
